# 前端页面统计监控系统

一个功能完善的前端页面访问统计与性能监控系统。

## 项目特性

- 🔐 **用户认证系统** - 完整的登录、注册、用户信息管理
- 📊 **数据统计** - PV/UV统计、访问趋势分析、页面热度排行
- ⚡ **性能监控** - 页面加载时间、资源性能、错误监控
- 🎯 **项目管理** - 多项目管理、灵活配置
- 📈 **可视化展示** - 基于 ECharts 的数据可视化

## 技术栈

### 后端
- Node.js + Express
- TypeORM + MySQL
- TypeScript
- JWT 认证

### 前端
- Vue 3 (Composition API)
- Vite
- Pinia (状态管理)
- ECharts (数据可视化)
- Axios
- Element Plus

### SDK
- 原生 JavaScript
- 轻量级、无依赖

## 快速开始

### 环境要求

- Node.js >= 16.x
- MySQL >= 8.0
- npm 或 yarn

### 安装

```bash
# 安装所有依赖
npm run install:all
```

### 配置

1. 复制环境变量配置文件：
```bash
cp .env.example .env
```

2. 修改 `.env` 文件中的数据库配置

3. 启动数据库（使用 Docker）：
```bash
docker-compose up -d mysql
```

### 启动开发服务器

```bash
# 启动后端服务
npm run dev:backend

# 启动前端服务
npm run dev:frontend
```

### 访问

- 前端管理系统: http://localhost:5173
- 后端 API: http://localhost:3000

## 使用方法

### 1. 注册账号

访问前端系统，注册一个新账号。

### 2. 创建项目

在项目管理页面创建一个新的监控项目，获取项目的唯一 Key。

### 3. 集成 SDK

在需要监控的前端项目中添加以下代码：

```html
<script src="http://your-domain.com/sdk.js?key=YOUR_PROJECT_KEY"></script>
```

### 4. 查看数据

登录系统，在统计分析页面查看访问数据和性能指标。

## 项目结构

```
web-page-monitoring/
├── backend/          # 后端服务
│   ├── src/
│   │   ├── entities/        # 数据模型
│   │   ├── controllers/     # 控制器
│   │   ├── routes/          # 路由
│   │   ├── middlewares/     # 中间件
│   │   ├── services/        # 业务逻辑
│   │   └── utils/           # 工具函数
│   └── package.json
├── frontend/         # 前端管理系统
│   ├── src/
│   │   ├── components/      # 组件
│   │   ├── views/           # 页面
│   │   ├── router/          # 路由
│   │   ├── store/           # 状态管理
│   │   └── api/             # API 请求
│   └── package.json
├── sdk/              # 数据采集 SDK
│   ├── src/
│   └── package.json
├── docker-compose.yml
└── README.md
```

## 功能模块

### 用户系统
- 用户注册
- 用户登录
- 信息修改
- 密码修改

### 项目管理
- 创建项目
- 编辑项目
- 删除项目
- 查看 SDK 集成代码

### 访问统计
- PV/UV 统计
- 访问趋势分析
- 页面热度排行
- 访客来源分析

### 性能监控
- 页面加载时间
- 资源加载性能
- 错误监控
- 性能评分

## 开发

### 后端开发

```bash
cd backend
npm run dev        # 开发模式
npm run build      # 构建
npm run start      # 生产模式
```

### 前端开发

```bash
cd frontend
npm run dev        # 开发模式
npm run build      # 构建
npm run preview    # 预览
```

### SDK 开发

```bash
cd sdk
npm run dev        # 开发模式
npm run build      # 构建
```

## 部署

使用 Docker Compose 一键部署：

```bash
docker-compose up -d
```

## 贡献

欢迎提交 Issue 和 Pull Request！

## 许可证

MIT License
